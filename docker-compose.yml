services:
    dss:
        image: ghcr.io/authentin/dss:latest
        ports:
            - '8080:8080'
        healthcheck:
            test: ['CMD-SHELL', 'wget -qO /dev/null http://localhost:8080/services/rest/validation || exit 1']
            interval: 10s
            timeout: 5s
            retries: 30
            start_period: 60s
